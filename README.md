# prettier-plugin-tailwindcss-format-demo

Minimal project prepared for replaying the error ( RangeError: Maximum call stack size exceeded ).

An error occurred when I formatted the code (apps\web\index.html) using this plugin, the error is **RangeError: Maximum call stack size exceeded**. Here is a link to a minimized project that can be run directly: [prettier-plugin-tailwindcss-format-demo](https://github.com/ASanny/prettier-plugin-tailwindcss-format-demo).

Prettier works fine when I downgrade to a lower version (such  as  **0.6.14**).
